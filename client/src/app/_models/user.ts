//Interfaces in TypeScript are little bit different to interfaces in C#.
//In typescript, we can use it to specify that something is a type of something.
export interface User{
    username : string;
    token: string;
}